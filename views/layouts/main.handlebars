<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    {{>styles}}

</head>
<body class="skin-blue">
<div class="wrapper">
    {{>receptionist-header}}

    {{>receptionist-sidebar}}
    <div class="content-wrapper">
        {{{body}}}
    </div>

    {{>footer}}
    {{>scripts}}
</div>

</body>
</html>
